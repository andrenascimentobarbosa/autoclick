LinkedIn clean up

This script removes LinkedIn connections.
If left running, the script will remove all your connections, so use it with caution.

I created this script to automate the repetitive task of removing connections on LinkedIn. It can also be adapted for other tasks. The script uses screenshots of icons instead of fixed cursor coordinates. Sample screenshots are provided, but you may need to take your own.
